/*
 * cargo new [project_name]
 * creates new git repo with gitignore
 * unless its part of another git repo
 *
 * besides uses .toml file like python
 *
 * there is also the cargo --init
 * also creates the toml file
 *
 * compling through -> cargo build
 * executable in a target/debug directory
 *          OR
 * cargo run -> compile & run
 *
 * cargo check -> without executable
 *
 * cargo build --release -> compile with optimizations
 * executable is in a target/release directory
*/
fn main() {
    println!("Hello, world!");
}
