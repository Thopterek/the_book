/*
 * Binary crates are programs (main)
 * Library crates are just C libs (no starting point)
 * Crate root is a source file that Rust compiler starts from
 * Package is a bundle of crates at most one Library and as many Binary
*/

fn main() {
    println!("Hello, world!");
}
