# Profiles of cargo

* modifed through flags of cargo build (base dev), second --release
    * possible addition inside the Cargo.toml to set the profile
    * if change it then through setting its profile.*
