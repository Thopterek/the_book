# Workspace

* set of packages that sharer the same Cargo.lock & output directory
* contained inside the add directory is a workspace with a binary and two libraries
* workspace is being through the Cargo.toml file, newest resolver = "3"
